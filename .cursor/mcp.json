{
  "mcpServers": {
    "firebase": {
      "command": "npx",
      "args": [
        "--registry=https://registry.npmjs.org",
        "-y",
        "firebase-tools@latest",
        "mcp",
        "--only",
        "core,auth,firestore,storage,messaging"
      ]
    }
  }
}
